# 동학 (DongHak)

국립순천대학교 학생의 4년을 함께하는 AI 학사 에이전트.

## 실행

```bash
npm install
npx vercel dev
```

브라우저에서 `http://localhost:3000` — API는 `http://localhost:3000/api/chat`.

계획 문서는 `docs/00_MASTER_PLAN.md`부터 순서대로 본다.
